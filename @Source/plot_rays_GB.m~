function h = plot_rays(source, grid, nColours)
% PLOT_RAYS Plots the ray trajectories for the given sources
%function h = plot_rays(source, grid, nColours)
% INPUTS
% source: source to plot the rays
% grid: gridRT object that defines the domain
% nColours: number of colours to plot
%
% OUTPUTS:
% h: figure handler
% 
% Copyright (C) 2017 Kiko Rul·lan, Marta M. Betcke
h = figure;
hold on;
axis([0 grid.xAxis(end) 0 grid.yAxis(end)]);
colourList = cool(nColours);
nRays = size(source.x, 1);
for j = 1:nRays
    colourNum = floor(nColours*(j-1)/nRays) + 1;
    plot(source.x(j, :, 1), source.x(j, :, 2), 'Color', colourList(colourNum, :));
end
xlabel('x (m)');
ylabel('y (m)');
title('Ray Trajectories');
